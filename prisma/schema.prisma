generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  username  String   @unique
  password  String
  nickname  String?
  role      String   @default("STUDENT")
  avatar    String?
  createdAt DateTime @default(now())
  name      String?
  courses   Course[]
}

model Course {
  id          Int      @id @default(autoincrement())
  title       String
  description String?
  cover       String?
  teacherId   Int
  createdAt   DateTime @default(now())
  teacher     User     @relation(fields: [teacherId], references: [id])
}

model PublishedExam {
  id           Int            @id @default(autoincrement())
  title        String
  subject      String
  difficulty   String
  questionType String
  createdAt    DateTime       @default(now())
  createdBy    String
  status       String         @default("active")
  questions    ExamQuestion[]
}

model ExamQuestion {
  id              Int            @id @default(autoincrement())
  question        String
  answer          String
  explanation     String?
  createdAt       DateTime       @default(now())
  subject         String
  difficulty      String
  questionType    String
  options         Json
  optionsHash     String?   // 可选，但推荐
  publishedExam   PublishedExam? @relation(fields: [publishedExamId], references: [id])
  publishedExamId Int?
  isCurrentExam   Boolean  @default(false) // 👈 新增字段！
  @@index([subject, difficulty, questionType])
  @@index([isCurrentExam]) // 👈 加索引，查询更快
}
